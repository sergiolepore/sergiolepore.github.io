<%
   var attr = '';
   if(type =="archive") {
     attr = 'span';
   } else if (type == "index" || type =="article") {
     attr = 'div';
   } %>
<% if(item.tags && item.tags.length) { %>
 <%
  var tags = [];
  item.tags.forEach(function(tag){
    tags.push('<a href="' + config.root + tag.path + '">' + tag.name + '</a>');
  });
 %>
 <<%- attr %> class="fa fa-tags">
   <%- tags.join(', ') %>
 </<%- attr %>>
<% } %>
